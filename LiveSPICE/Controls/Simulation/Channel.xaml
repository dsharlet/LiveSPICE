<UserControl x:Class="LiveSPICE.Channel" x:Name="this"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="300">
    <DockPanel LastChildFill="True">
        <TextBlock x:Name="name" DockPanel.Dock="Left" Width="50" Margin="0, 0, 8, 0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />

        <ComboBox DockPanel.Dock="Left" Width="36" ItemsSource="{Binding ElementName=this, Path=Signals}" 
                  SelectedValuePath="Tag" SelectedValue="{Binding ElementName=this, Path=Signal}"
                  ToolTip="{Binding ElementName=this, Path=Signal}" />
        
        <TextBlock x:Name="level" DockPanel.Dock="Right" Width="45" Margin="1" 
                   TextAlignment="Center" FontWeight="Bold" Text="{Binding ElementName=this, Path=Gain, StringFormat={}{0:+#;-#;+0 dB}}" />

        <Slider Value="{Binding ElementName=this, Path=Gain}" Minimum="-20" Maximum="20" />
    </DockPanel>
</UserControl>
