<UserControl x:Class="LiveSPICE.PropertyGrid" x:Name="_this"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel LastChildFill="True" x:Name="content">
        <ComboBox DockPanel.Dock="Top" ItemsSource="{Binding ElementName=_this, Path=Objects}" SelectedValue="{Binding ElementName=_this, Path=SubSelectedObject}" SelectedValuePath="Tag" />
    </DockPanel>
</UserControl>
